# 模拟法

## 基本思想

模拟法的核心在于**严格按照题目描述的过程逐步执行**，不需要特殊的算法技巧或优化思路。这类题目主要考察：
- 对题目规则的准确理解
- 边界条件的仔细处理
- 代码的清晰组织能力

模拟题通常没有高深的算法思想，但容易因为**漏掉细节或边界**而出错。

## 模拟法的要点

1. **仔细读题**：理解每一步操作的确切含义
2. **明确状态**：记录哪些变量，状态如何变化
3. **处理边界**：循环终止条件、数组越界、负数处理等
4. **分步实现**：将复杂过程分解为独立的小步骤

## 经典实例：螺旋矩阵 (54)

**题目**：给你一个 m 行 n 列的矩阵，按顺时针螺旋顺序返回所有元素。

**模拟策略**：按照"向右→向下→向左→向上"的顺序循环移动，每走完一个方向就缩短边界。

```java
public List<Integer> spiralOrder(int[][] matrix) {
    List<Integer> result = new ArrayList<>();
    int top = 0, bottom = matrix.length - 1;
    int left = 0, right = matrix[0].length - 1;
    while (top <= bottom && left <= right) {
        // 向右
        for (int j = left; j <= right; j++) result.add(matrix[top][j]);
        top++;
        // 向下
        for (int i = top; i <= bottom; i++) result.add(matrix[i][right]);
        right--;
        if (top <= bottom) {
            // 向左
            for (int j = right; j >= left; j--) result.add(matrix[bottom][j]);
            bottom--;
        }
        if (left <= right) {
            // 向上
            for (int i = bottom; i >= top; i--) result.add(matrix[i][left]);
            left++;
        }
    }
    return result;
}
```

## 相关 LeetCode 题目

| 编号 | 题目 | 难度 | 备注 |
|:----:|------|:----:|------|
| 13 | 罗马数字转整数 | 简单 | 字符模拟 |
| 67 | 二进制求和 | 简单 | 逐位计算 |
| 228 | 汇总区间 | 简单 | 区间模拟 |
| 54 | 螺旋矩阵 | 中等 | 方向控制 |
| 59 | 螺旋矩阵 II | 中等 | 生成矩阵 |
| 73 | 矩阵置零 | 中等 | 原地标记 |
| 289 | 生命游戏 | 中等 | 状态模拟 |
| 68 | 文本左右对齐 | 困难 | 复杂格式模拟 |

> 代码目录：`Algorithm-Cote/src/leetCode/`
